<% visiter = notification.visiter %>
<% post = notification.post %> <!-- review を post に変更 -->

<ul class="list-none pl-0">
  <li class="text-gray-700">
    <%= visiter.name %>さんが
    <% if post %>
      <%= link_to "あなたの投稿した投稿", post_path(post), class: "text-blue-500 hover:text-blue-700 underline" %>にコメントしました。
    <% else %>
      投稿が削除されたか、存在しないかもしれません。
    <% end %>
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </li>
</ul>

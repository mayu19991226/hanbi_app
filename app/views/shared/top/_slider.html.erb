<!DOCTYPE html>
<html lang="en">
<head>  
  <!-- CDNでSlickを読み込む -->
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css">
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick-theme.css">
  
  <!-- カスタムCSS -->
  <style>
    .your-class img {
      max-height: 350px;       /* 高さを最大350pxに設定 */
      width: 100%;             /* 幅を100%に設定 */
      object-fit: contain;     /* 画像全体を表示 */
    }
    
    .slick-slide {
      display: flex;           /* フレックスボックスを使用 */
      justify-content: center; /* 中央揃え */
      align-items: center;     /* 中央揃え */
    }

    .container {
      width: 1000px;           /* コンテナの幅を1000pxに設定 */
      margin: 0 auto;          /* 中央揃え */
      display: flex;           /* フレックスボックスを使用 */
      justify-content: center; /* 中央揃え */
      align-items: center;     /* 中央揃え */
    }

    .slick-slider {
      width: 100%;             /* スライダーの幅を100%に設定 */
    }

    /* メディアクエリでスマホサイズに対応 */
    @media (max-width: 768px) {
      .container {
        width: 100%;           /* スマホサイズでのコンテナ幅を100%に設定 */
      }
      .your-class img {
        max-height: 200px;     /* スマホサイズでの高さを調整 */
      }
    }
  </style>
</head>

<body>
  <!-- トップページのコンテンツ部分にスライダーを追加 -->
  <div class="container mx-auto">
    <div class="your-class">
      <div><img src="/assets/slider1.png" alt="Slide 1"></div>
      <div><img src="/assets/slider2.png" alt="Slide 2"></div>
      <div><img src="/assets/slider3.png" alt="Slide 3"></div>
      <!-- 追加のスライド -->
    </div>
  </div>

  <!-- jQuery -->
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
  <!-- Slick JavaScript -->
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>

  <!-- Slick初期化スクリプト -->
  <script type="text/javascript">
    $(document).ready(function(){
      $('.your-class').slick({
        autoplay: true,          // 自動再生を有効化
        autoplaySpeed: 3000,     // スライドが切り替わる間隔（ミリ秒）
        dots: true,              // ドットナビゲーションを表示
        infinite: true,          // 無限ループ
        speed: 500,              // スライドが切り替わる速度（ミリ秒）
        slidesToShow: 1,         // 一度に表示するスライド数
        slidesToScroll: 1        // スクロール時に切り替わるスライド数
      });
    });
  </script>
</body>
</html>
